jenkins:
  systemMessage: "MODO DE PRUEBA: Seguridad desactivada para depuración."
  # La sección de seguridad se ha comentado temporalmente para la prueba
  # securityRealm:
  #   local:
  #     allowsSignup: false
  #     users:
  #       - id: "admin"
  #         name: "Admin"
  #         password: "${ADMIN_PASSWORD}"
  # authorizationStrategy:
  #   loggedInUsersCanDoAnything:
  #     allowAnonymousRead: false
  clouds:
    - docker:
        name: "docker"
        dockerApi:
          dockerHost:
            uri: "unix:///var/run/docker.sock"
        templates:
          - name: "terraform-agent"
            labelString: "terraform-agent"
            remoteFs: "/home/jenkins/agent"
            instanceCapStr: "1"
            connector:
              attach: {}
            dockerTemplateBase:
              image: "my-terraform-agent:latest"
